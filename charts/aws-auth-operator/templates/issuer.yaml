apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  labels:
    {{- include "aws-auth-operator.labels" . | nindent 4 }}
  name: aws-auth-operator-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
spec:
  selfSigned: {}